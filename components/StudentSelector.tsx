import React from 'react';

// This component is no longer used as per the user's request to simplify
// the SeguroGenerator to only work by "convocatoria".
const StudentSelector: React.FC = () => {
  return null;
};

export default StudentSelector;
